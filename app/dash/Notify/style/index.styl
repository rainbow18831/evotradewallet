.notify
  width 100%
  height 100%
  pointer-events auto
  transition var(--standard)
  display flex
  justify-content center
  align-items center
  text-align center
  opacity 1
  border-radius 10px
  background #002739
  
  ::selection
    background var(--moon)

  .notifyCloseButton
    position absolute
    top 23px
    width 100px
    margin-left -50px
    left 50%
    height 25px
    border-radius 8px
    display flex
    justify-content center
    align-items center
    text-transform uppercase
    font-size 11px
    font-weight 300
    cursor pointer
    background #002739
    color var(--moon)

    *
      pointer-events none

  .notifyBoxWrap
    border-radius 27px
    box-sizing border-box
    position absolute
    top 6px
    bottom 6px
    left 6px
    right 6px
    transition var(--standard)
    background #0C3344
    display flex
    justify-content center
    align-items center
    text-align center
    opacity 1
    overflow-x hidden
    z-index 4000

    .notifyBoxSlide
      overflow-y scroll
      overflow-x hidden
      max-height 100%
      width 100%

    .notifyClose
      position absolute
      top 0px
      right 0px
      color var(--outerspace)
      z-index 3
      height 40px
      width 40px
      display flex
      justify-content center
      align-items center

    .notifyBox
      border-top 1px solid #0C3344
      border-bottom 1px solid #0C3344
      width 100%
      overflow hidden
      padding 64px 0px
      
      .notifyTitle
        font-size 19px
        position relative
        z-index 3
        margin-bottom 40px
        color var(--moon)

      .notifySubtitle
        font-size 16px
        position relative
        z-index 3
        margin-bottom 24px

      .notifyFrameIcon
        height 106px 
        width 100%
        position relative
        z-index 4
        display flex
        justify-content center
        align-items center

        img
          width 96px
          height 96px
          border-radius 24px

      .notifyBody
        font-size 17px
        position relative
        z-index 3
        padding 4px
        word-break break-word
        width 100%
        box-sizing border-box
        padding 20px

        .notifyBodyLine
          padding 0px 0px 
          text-transform uppercase
          font-size 16px
          font-weight 300
          font-family 'MainFont'

        .notifyBodyLineUrl
          padding-bottom 56px
          font-family 'FiraCode'
          font-size 16px
          word-break break-all
 
        .notifyBodyUpdate
          padding 8px
          margin-bottom 8px
          background var(--moon)
          color var(--moonOver)
          border-radius 8px
          margin-top 50px
          font-size 13px
          font-weight 300

        .notifyBodyBlock
          padding 8px
          margin-bottom 8px
          background #002739
          border-radius 12px
          border 3px solid #002739

        .notifyBodyBlockBig
          padding 32px
          font-size 16px
          background #002739

        .notifyBodyLink
          text-decoration underline
          cursor pointer

        .notifyBodyHash
          font-size 14px
          font-family 'FiraCode'
          background #002739
          line-height 20px
          box-shadow inset 0px 2px 14px #002739
          padding 18px
          border-radius 12px
          margin-top 10px
          border 1px solid #0C3344
          text-shadow 0px 2px 0px #002739
          min-height 58px
          max-height 58px

        .notifyBodyPrice
          margin-top 4px
          font-size 23px

        .notifyBodyQuestion
          padding-top 40px
          color var(--moon)

      .notifyCheck
        display flex
        position relative
        justify-content center
        align-items center
        font-weight 400
        z-index 3
        margin-top 16px
        margin-left 8px
        padding 0px 30px

        .notifyCheckBox 
          width 32px
          height 32px
          min-width 32px
          min-height 32px
          border-radius 16px
          background #0C3344
          border 1px solid #002739
          box-shadow cardDrop, 0px 2px 1px #0C3344, 0px 0px 7px 2px #002739
          margin-right 10px
          box-sizing border-box
          display flex
          position relative
          justify-content center
          align-items center
          cursor pointer
          *
            pointer-events none

          svg 
            height 22px

        .notifyCheckBox:hover
          background var(--ghostD)

        .notifyCheckText
          text-align left
          font-size 13px
          line-height 13px
          width 210px

      .notifyInput
        display flex
        position relative
        justify-content space-between
        z-index 3
        margin 40px 8px 0px 8px

        .notifyInputOption
          position relative
          width calc(50% - 1px)
          display flex
          justify-content center
          align-items center
          box-sizing border-box
          background #0C3344
          height 40px
          font-weight 300
          padding-top 1px
          box-shadow cardDrop, 0px 2px 1px #0C3344, 0px 0px 7px 2px #002739
          text-transform uppercase
          font-size 14px
          font-weight 400
          letter-spacing 1px
          cursor pointer
          *
            pointer-events none

          .notifyInputOptionText
            position relative
            z-index 200
            padding-bottom 2px

        .notifyBetaGo
          color var(--good)

        .notifyInputOption:hover:before
          background var(--ghostD)

        .notifyInputDeny:before
          content ''
          position absolute
          top 0
          left 0
          right 0
          bottom 2px
          background #002739
          z-index 2
          border-top-left-radius 19px
          border-bottom-left-radius 19px

        .notifyInputDeny
          border-top-left-radius 20px
          border-bottom-left-radius 20px

        .notifyInputProceed:before
          content ''
          position absolute
          top 0
          left 0
          right 0
          bottom 2px
          background #002739
          z-index 2
          border-top-right-radius 19px
          border-bottom-right-radius 19px

        .notifyInputProceed
          border-top-right-radius 20px
          border-bottom-right-radius 20px

        .notifyInputSingleButton
          left 50%
          transform translate(-50%, 0)
          border-radius 12px
          height 50px
          background #002739
          background #002739
          border 3px solid #002739

    .introInstructions
      position relative
      z-index 3
      margin -10px
      font-size 12px
      font-weight 300
      border-radius 6px
      text-align cleft
      line-height 18px
      font-weight 300
      color var(--outerspace08)

      .introInstructionItem
        margin-bottom 20px
        padding 0px 20px
        span
          color var(--pending)
          text-decoration none

      .introInstructionList
        margin 18px 5px 30px 5px
        border-radius 6px
        padding 16px
        font-weight 400
        background var(--pending)
        color var(--spacewhite)
        div
          font-size 14px
          line-height 26px
